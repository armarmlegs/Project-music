
{{> navbar}}

<h1>All albums</h1>

<div class="cards">
    {{#each albums}}
    <div class="card album" style="background-image: url({{cover}}">
        <a href="/albums/albums/{{this._id}}">
            {{this.title}}
        </a>
    </div>
    {{/each}}
</div>